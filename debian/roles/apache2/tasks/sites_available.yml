---
- name: Create /etc/apache2/sites-available
  ansible.builtin.file:
    path: /etc/apache2/sites-available
    state: directory
    owner: root
    group: root
    mode: '0755'

- name: Create apache2 site configuration files
  ansible.builtin.copy:
    content: "{{ item.value }}"
    dest: "/etc/apache2/sites-available/{{ item.key }}"
    owner: root
    group: root
    mode: '0644'
  loop: "{{ apache2_sites_available | dict2items }}"
  notify:
    - Restart apache2
