---
- name: Create /etc/systemd/system/sshd.service.d
  ansible.builtin.file:
    path: /etc/systemd/system/sshd.service.d
    state: directory
    owner: root
    group: root
    mode: '0755'

- name: Create /etc/systemd/system/sshd.service.d/override.conf
  ansible.builtin.copy:
    content: "{{ sshd_systemd_override }}"
    dest: /etc/systemd/system/sshd.service.d/override.conf
    owner: root
    group: root
    mode: '0644'
  notify:
    - Restart sshd
